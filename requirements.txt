# Core metadata / audio reading
mutagen>=1.47.0

# Vocal source separation (htdemucs model)
demucs>=4.0.0

# Forced alignment (install from GitHub for latest features)
# pip install git+https://github.com/m-bain/whisperX.git
whisperx>=3.1.1

# Deep-learning backend (GPU support: install the CUDA wheel manually if needed)
torch>=2.0.0
torchaudio>=2.0.0

# Progress display
tqdm>=4.65.0

# Numerical processing (whisperx transitive dependency, listed explicitly)
numpy>=1.24.0
